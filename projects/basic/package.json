{
  "name": "basic",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --mode client",
    "preview": "wrangler pages dev ./dist",
    "deploy": "$npm_execpath build && wrangler pages deploy ./dist"
  },
  "private": true,
  "dependencies": {
    "hono": "./hono-4.0.0-rc.0.tgz"
  },
  "devDependencies": {
    "honox": "^0.0.0",
    "vite": "^5.0.10",
    "wrangler": "^3.22.1"
  },
  "packageManager": "yarn@4.0.2"
}